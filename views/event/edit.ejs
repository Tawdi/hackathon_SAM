<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Éditer l'Événement</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1>Éditer l'Événement</h1>
    <form action="/events/edit/<%= event.id %>" method="POST">
        <label for="name">Nom:</label>
        <input type="text" id="name" name="name" value="<%= event.name %>" required>
        <label for="description">Description:</label>
        <textarea id="description" name="description"><%= event.description %></textarea>
        <label for="location">Lieu:</label>
        <input type="text" id="location" name="location" value="<%= event.location %>">
        <label for="start_date">Début:</label>
        <input type="datetime-local" id="start_date" name="start_date" value="<%= event.start_date.toISOString().slice(0, 16) %>" required>
        <label for="end_date">Fin:</label>
        <input type="datetime-local" id="end_date" name="end_date" value="<%= event.end_date.toISOString().slice(0, 16) %>" required>
        <button type="submit">Mettre à Jour</button>
    </form>
</body>
</html>
